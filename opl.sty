\usepackage{listings}

\lstdefinelanguage{opl}{
    morekeywords={all, and, assert, boolean, constraint, constraints, cp, cplex, cumulFunc, DBConnection, DBExecute, DBRead, DBUpdate, dexpr, diff, div, dvar, else, execute, false, float, floatplus, forall, from, if, in, include, infinity, int, intplus, intensity, inter, interval, invoke, key, main, max, maximize, maxint, min, minimize, mod, not, optional, or, ordered, piecewise, prepare, prod, pwlFunc, range, reversed, sequence, setof, SheetConnection,
SheetRead, SheetWrite, size, sorted, SPSSConnection, SPSSRead, stateFunc, stepFunc, stepwise, string, subject, sum, symdiff, to, true, tuple, types, union, using, with},
    sensitive=true,
    morecomment=[l]{//},
    morecomment=[s]{/*}{*/},
    morestring=[b]",
}
